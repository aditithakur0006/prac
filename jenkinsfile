pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh '/var/lib/jenkins/workspace/github1/add.sh'
            }
        }
    }
}
