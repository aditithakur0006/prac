pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh "chmod +x add.sh"
                sh '/var/lib/jenkins/workspace/github1/add.sh'
            }
        }
    }
}
